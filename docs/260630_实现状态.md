# 实现状态对照

> 更新：2026-06-30 · **权威进度表**

## 控制面

| 组件 | 状态 | 路径 / 端口 |
|------|:---:|-------------|
| Client Gateway | ✅ | `services/client-gateway/` · 8010 |
| Playbook Orchestrator | ✅ | `services/orchestrator/` · 8020 |
| Capability Registry | ✅ | `services/capability-registry/` · 8021 |
| Planner Agent | ✅ ReAct | `services/planner-agent/` · 8011 |

## 业务 Agent

| Agent | 类型 | 状态 | 端口 |
|-------|------|:---:|:---:|
| quality-rca-agent | LangGraph + FMEA + HITL | ✅ | 8003 |
| report-reporter-agent | Deep Agents + 模板 fallback | ✅ | 8004 |
| trace-worker | MCP 取证（无 LLM） | ✅ | 8002 |
| triage-agent | 意图识别 + 异常分诊（LLM + Rule 双模） | ✅ | 8001 |
| patrol / quality-pred / process-opt / equipment-health / safety / wms-supply | AgentCard 已注册 | ⏸️ stub | 8005/8201/8202/8203/8099/8204 |

## 平台能力

| 组件 | 状态 |
|------|:---:|
| A2A 协议（tasks/send + resume + AgentCard） | ✅ |
| PlatformContext 黑板（含 artifacts 通用扩展槽） | ✅ |
| EventBus SSE | ✅ |
| HITL 签核回写 | ✅ |
| RCA checkpoint (MemorySaver) | ✅ |
| MCP 六域 (mes/scada/erp/lims/qms/knowledge) | ✅ |
| AgentNetwork 动态寻址 | ✅ |
| Playbook 4 个 (trace_only / investigate / rca / close_loop) | ✅ |

## 工程化 & 韧性

| 能力 | 状态 | 位置 |
|------|:---:|------|
| RCA 兜底策略（失败/超时→构造初判 root_cause） | ✅ | `orchestrator/app.py:_fallback_rca` |
| LangGraph 超时 (300s) + recursion_limit (50) | ✅ | `api/handlers.py` |
| Token 用量追踪 (prompt/completion/total) | ✅ | `agent/llm.py:_log_usage` |
| MCP 熔断器 (3 次失败→OPEN→5%探针率恢复) | ✅ | `harness_core/resilience/circuit_breaker.py` |
| Prompt 版本化管理 (YAML registry + golden_set + metrics) | ✅ | `packages/eval-core/prompts/registry.yaml` |
| Agent Registry 心跳探活 (30s 循环) | ✅ | `capability-registry/app.py` |
| Playbook YAML 配置 | ✅ | `config/playbooks.yaml` |
| 离线评测 (规则 Judge + LLM-as-Judge + Golden Set) | ✅ | `packages/eval-core/` · `scripts/run_eval.py` |
| 指数退避重试 | ✅ | `harness_core/resilience/retry.py` |
| RBAC + trace_id 审计 | ✅ | `harness_core/permission/` · `audit/` |

## 已知限制

| 项 | 说明 |
|----|------|
| Orchestrator→RCA 同步调用 | LangGraph 耗时 > HTTP timeout 时走兜底策略 |
| 三层记忆 | ✅ STM(Redis→InMemory) · Working(PG→InMemory) · LTM(Milvus+Neo4j→InMemory) 全链路 fallback |
| 向量+图混合检索 | ✅ `knowledge.hybrid_search_golden_case` MCP 已实现；15 条 Golden Case 待灌入 |
| RCA 端到端评测 | Golden Set 仅 2 例，需真实产线 case 扩充 |
| 多工厂 tenant | 未实现 |
| Playbook DSL 引擎 | ✅ PlaybookEngine 已实现，Orchestrator 使用中；新增剧本只需加 YAML + AgentCard |

## 生产推荐 env

```env
ORCHESTRATOR_URL=http://127.0.0.1:8020
PLANNER_URL=http://127.0.0.1:8011
CONTEXT_BACKEND=postgres          # memory | redis | postgres
EVENT_BACKEND=redis
REDIS_URL=redis://localhost:6379/0
POSTGRES_DSN=postgresql://battery:battery@localhost:5432/battery_agent
HTTP_TIMEOUT=600
```
