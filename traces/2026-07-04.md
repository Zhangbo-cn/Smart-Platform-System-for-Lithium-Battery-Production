# 2026-07-04 会话留痕

## 核心架构变更

### 1. a2a_server.py Redis + streaming
- Redis 持久化 `_thread_map`（session_id↔thread_id），重启不丢 HITL
- `handle_task` 加 streaming 分支（`astream_events` + `event_callback`）
- 兼容 `__interrupt__` 返回值模式和异常模式两种 LangGraph 版本

### 2. RcaAgentServer(AsyncA2AServer)
- 重写 `rca-agent/api/main.py`，删除自建 HITL 框架
- `build_input` / `extract_output` / `handle_task` / `handle_resume` 标准化
- 中断检测从 `is_interrupted(final)`（返回值）改为 __interrupt__ 检测 + 异常兜底

### 3. 删除冗余文件（4个）
- `harness/hitl/broker.py`（HITLBroker → _thread_map）
- `api/a2a_hitl.py`（格式转换 → handle_resume）
- `api/task_store.py`（InMemoryTaskStore → SessionStore）
- `api/handlers.py` 大部（逻辑移到 RcaAgentServer）

### 4. SmartRouter
- 新增 `platform-contracts/smart_router.py`，Orchestrator LLM 智能路由
- 默认关闭（`enable_smart_routing=false`），YAML 兜底
- 直调 LLM API（不走 A2A），修复了原来错误走 Planner 端口的问题

### 5. 评测体系
- `eval-core/scorers.py` — RuleScorer + LLM Judge，覆盖 4 Agent
- 数据集：RCA 17例、Reporter 15例、Planner 6例、Orchestrator 5例
- `scripts/run_agent_eval.py` — 统一评测 CLI

### 6. Phase 6 — 6 个扩展 Agent 模板化
- patrol / quality-prediction / process-optimization / equipment-health / wms-supply / safety
- 全部继承 AsyncA2AServer，全部 enabled=True
- MCP：EAM(8107)、WMS(8108)、PLC(8110) 加入 docker-compose

### 7. 修复
- PLC 端口 8106→8110（与 Knowledge 冲突）
- MES get_shift_summary "planned"→"implemented"
- docker-compose 补齐全部 16 服务

### 决策记录
- SmartRouter 直接调 LLM API，不走 A2A（之前错误走 Planner 端口）
- RCA 中断检测用 __interrupt__ 返回值而非异常（适配 RCA 的 LangGraph 版本）
- 评测的确定性评分权重 60、LLM Judge 30、效率 10（门禁 80 分）

### 用户训练面试
- 逐条过简历，覆盖中台架构/RCA Agent/Reporter Agent/项目成果
- 准备了 Q&A 口径（A2A 不是传输协议、置信度量化公式、HITL checkpoint 恢复）
